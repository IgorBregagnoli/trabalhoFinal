const express = require("express");
const router = express.Router();

router.post("/:personagemId/habilidades/:habilidadeId", (req, res) => {

});

router.delete("/:personagemId/habilidades/:habilidadeId", (req, res) => {

});

module.exports = router;